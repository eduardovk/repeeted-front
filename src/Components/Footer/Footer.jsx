import React, { useState } from 'react';
import "./style.css";

function Footer() {

    const [showMenu, setShowMenu] = useState(false);

    return (
        <footer>
            
        </footer>
    );

}
export default Footer;